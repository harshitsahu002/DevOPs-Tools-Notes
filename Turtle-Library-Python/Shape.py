import turtle
t= turtle.Turtle()
t.shape() # to get the default shape of the cursor which is 'classic'
t.shape("turtle")# to change the shape of the cursor

# To get the full informatio about the full information of the shape option in turtle library
Type the following command in the shell
>> import turtle
>> help(turtle.shape)
