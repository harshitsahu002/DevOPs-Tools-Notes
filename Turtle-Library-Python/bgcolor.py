import turtle 
t = turtle.Turtle()
t.color("red")
t.shape("turtle")
wn = turtle.Screen()
wn.bgcolor("black")

# to get the colormode of the screen type the following command in the shell
# >>> turtle.colormode()

turtle.colormode(255) # this colormode is set for the RGB mode
wn.bgcolor(170,30,20)
